name: Tile Expert Test Tasks

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run code
        run: echo "Tile Expert Test Tasks"